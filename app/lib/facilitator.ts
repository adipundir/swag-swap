// Facilitator configuration
// Using x402.org public facilitator (no API keys required)

export const facilitatorConfig = {
  url: "https://x402.org/facilitator" as `${string}://${string}`
};

export const facilitatorUrl = facilitatorConfig.url;
